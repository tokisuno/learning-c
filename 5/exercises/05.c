#include <stdio.h>

int main(void)
{
  printf("Yes. The program will execute the print statement\n");

  // left-associativity equal test operators
  // n >= 1 returns 0 (false), but 0 <= 10 returns 1 (true)

  return 0;
}
